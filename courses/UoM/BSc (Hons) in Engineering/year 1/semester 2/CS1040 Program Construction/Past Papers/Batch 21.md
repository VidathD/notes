# Batch 21 (2023)

## Question 1

### Part C

1. During compile time, no physical memory is used. However, the compiler does note that `int i` will require 4 bytes are `Integer intObj` will require a 4 byte reference. It reserves this pace in the stack frame of the executable created. 